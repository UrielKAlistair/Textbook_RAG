def generate_embeddings(file_name):
    # Check if outputs/file_name folder exists and it is not empty.
    # If so, return.
    return

    # Use semantic splitting to chunk, store in vectorDB within same directory
